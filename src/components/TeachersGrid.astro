---
import TeacherCard from './TeacherCard.astro'
const { teachers = [], showImage = true } = Astro.props
---

<section>
  <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4">
    {teachers.map((name) => <TeacherCard teacher={name} {showImage} />)}
  </div>
</section>
